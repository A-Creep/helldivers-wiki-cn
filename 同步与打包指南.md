# 绝地潜兵2 中文百科 · 同步与打包指南

> 项目根：`G:\Codex项目\helldivers-wiki-localizer`
> 发布镜像仓库：`G:\Codex项目\helldivers-wiki-mirror`（origin = `https://github.com/A-Creep/helldivers-wiki-cn.git`，GitHub Pages）

本文档覆盖：完整构建管线、增量同步流程、一键脚本 `sync_update.py`、镜像推送、EXE 打包，以及常见问题速查。

---

## 1. 前置条件

- Python 3.10+（本机 3.11.9），已安装 `requests`、`PyInstaller`、`pythonnet` 等依赖
- 访问境外站点/推 GitHub 必须走代理 `http://127.0.0.1:7890`（Clash for Windows）
  - 检查代理：`Test-NetConnection 127.0.0.1 -Port 7890 -WarningAction SilentlyContinue`
  - 未启动时：`Start-Process "G:\桌面文件\Clash\Clash for Windows.exe"`
  - 本机 hosts 被 Watt Toolkit 改过，`github.com` 指向 `127.0.0.1`，**直连推送必失败（502/CONNECT 404）**
- 镜像仓 git 身份未设全局，提交前需在仓库内配置（`sync_update.py --push` 会自动配置）：
  ```powershell
  git config user.name "A-Creep"
  git config user.email "A-Creep@users.noreply.github.com"
  ```

---

## 2. 完整管线命令序列与耗时

### 2.1 一次性全量初始化（新环境/缓存全丢时）

| 步骤 | 命令 | 说明 | 参考耗时 |
|------|------|------|----------|
| 1. 初始化 DB | `python wiki_extractor.py init` | 建表 | 秒级 |
| 2. 全量同步 | `python wiki_extractor.py sync --refresh-excludes` | 拉取全部 HD2 页面 wikitext（约 1900+ 内容页，排除一代/非英语） | 约 10~20 分钟 |
| 3. 提取待译文本 | `python wiki_extractor.py extract` | 生成 `untranslated.json` | 约 2 分钟 |
| 4. 导入翻译 | `python wiki_extractor.py import translated.json` | 已有翻译可多文件重复导入 | 秒级~1 分钟 |
| 5. 生成汉化 wikitext | `python wiki_extractor.py build` | 写 `output_zh/*.wiki.txt` | 约 1 分钟 |
| 6. 抓取原站镜像 | `python build_mirror.py` | 拉取渲染 HTML + 图片到 `output_zh/site`（约 1 万张图） | 约 1~2 小时 |
| 7. 图片压缩 | `python compress2.py` 或 `python compress4.py` | 大图转 WebP、删未引用图 | 约 10~30 分钟 |
| 8. 汉化渲染站点 | `python build_site.py` | 读 `zh_cache.jsonl` 重写渲染约 1146 页（无缓存时全量） | **全量约 25 分钟** |
| 9. 清除 HD1 残留 | `python strip_hd1.py` | 删除一代提示块/navbox/章节 | 约 1 分钟 |
| 10. 术语汉化 | `python apply_zh_terms.py` | 替换模板/UI 英文 | 约 1 分钟 |
| 11. 生成分类索引页 | `python generate_index_pages.py` | 重建 `categories/*.html`、更新首页 NAV | 约 30 秒 |
| 12. 链接检查 | `python verify_links.py` | 全站约 17.7 万链接 | 约 1~2 分钟 |
| 13. 健康度校验 | `python validate_site.py --report` | 高危=0 才算通过 | 约 1~2 分钟 |
| 14. 打包 EXE | `python -m PyInstaller --noconfirm --clean Helldivers2WikiCN.spec` | 产物 `dist/Helldivers2WikiCN.exe`（约 487MB） | 约 5~10 分钟 |
| 15. 镜像同步+推送 | 见第 5 节 | robocopy + git push | 约 5~15 分钟 |

### 2.2 日常增量（原站内容更新后）

| 步骤 | 命令 | 说明 | 参考耗时 |
|------|------|------|----------|
| 1. 增量同步 | `python wiki_extractor.py sync` | 按 revid 对比只拉变更页 | 约 5~20 秒 |
| 2. 只重渲染变更页 | 删除变更页的 `zh_cache.jsonl` 行后 `python build_site.py` | 缓存命中其余页面 | **约 6 分钟** |
| 3. 后处理 | strip → terms → index | 同 2.1 第 9~11 步 | 约 2~3 分钟 |
| 4. 验证 | verify_links + validate_site | 0 高危 | 约 2~4 分钟 |
| 5. 打包/推送 | 见第 3、5 节 | 可选 | — |

> 说明：`build_site.py` 的缓存以 `output_zh/site_final/zh_cache.jsonl` 为准，只渲染**缓存中缺失**的页面。删掉变更页的缓存行即可实现"只重渲染变更页"。

---

## 3. 一键增量同步：`sync_update.py`

将上述"检测变更页 → 删缓存行 → 重建 → 验证 → 打包 → 镜像推送"封装为一个脚本，默认走 7890 代理。

### 3.1 常用用法

```powershell
# 日常增量：同步 + 只重渲染变更页 + 链接检查 + 健康度校验
python sync_update.py

# 全量发布：上面的流程 + 打包 EXE + 镜像同步推 GitHub
python sync_update.py --all

# 只打包不推送
python sync_update.py --package

# 只推镜像不打包
python sync_update.py --push

# 离线修复/测试：不联网，按现有 DB/缓存重建
python sync_update.py --skip-sync

# 强制全量同步 + 清空缓存全量重建
python sync_update.py --force

# 强制重渲染某几个页面（不联网）
python sync_update.py --skip-sync --invalidate "Assault Rifle" --invalidate "Sickle"
```

### 3.2 参数一览

| 参数 | 作用 |
|------|------|
| `--skip-sync` | 跳过联网同步，仅按现有 DB/缓存重建（离线修复/测试） |
| `--force` | 强制全量同步，并清空渲染缓存全量重建 |
| `--refresh-excludes` | 同步前重建一代内容排除列表 |
| `--invalidate 标题` | 强制重渲染指定页面（可重复） |
| `--no-verify` | 跳过链接检查 |
| `--no-validate` | 跳过健康度校验 |
| `--package` | 校验通过后打包离线 EXE |
| `--push` | 镜像同步到 mirror 仓库并推 GitHub（走代理） |
| `--all` | `--package --push` 一起 |
| `--mirror 路径` | 镜像仓库路径（默认 `G:\Codex项目\helldivers-wiki-mirror`） |
| `--proxy URL` | 代理地址（默认 `http://127.0.0.1:7890`） |
| `--no-proxy` | 直连，不走代理 |
| `--message 文本` | 自定义 git 提交信息 |

### 3.3 行为说明

- **无变更时秒级退出**：只做同步检测，跳过重建/打包/推送。
- **有变更时只重渲染变更页**：删除变更页缓存行 → `build_site.py` 只渲染缺失缓存（其余页面命中缓存，约 6 分钟）。
- **校验前置**：`verify_links.py` 有缺失链接、或 `validate_site.py` 有高危项时，流程终止，不会进入打包/推送。
- **打包前自动结束占用进程**：`taskkill /IM Helldivers2WikiCN.exe /F`（best-effort）。
- **推送前自动配置 git 身份**：`A-Creep <A-Creep@users.noreply.github.com>`（仓库级）。

---

## 4. 手动等效命令序列

`python sync_update.py` 等价于依次执行：

```powershell
cd G:\Codex项目\helldivers-wiki-localizer
$env:HTTP_PROXY = 'http://127.0.0.1:7890'; $env:HTTPS_PROXY = 'http://127.0.0.1:7890'

python wiki_extractor.py sync
# （有变更时）删除 zh_cache.jsonl 中对应变更页的行
python build_site.py
python strip_hd1.py
python apply_zh_terms.py
python generate_index_pages.py
python verify_links.py
python validate_site.py --report
```

`--all` 在此基础上追加：

```powershell
taskkill /IM Helldivers2WikiCN.exe /F 2>$null   # 若有进程占用
python -m PyInstaller --noconfirm --clean Helldivers2WikiCN.spec
# 镜像同步见第 5 节
```

> 注意：`build_exe.ps1` 里的 `--add-data "output_zh/site;site"` 是旧路径；当前产物以 `Helldivers2WikiCN.spec`（`output_zh/site_final`）为准。

---

## 5. 镜像同步与推送（GitHub Pages）

### 5.1 robocopy 同步

镜像仓库根目录同时保留着 README/PROGRESS/辅助脚本，因此**不要对根目录做 `/MIR`**（会把它们删掉）。只对站点管理的三个子目录做镜像，顶层再拷贝站点文件：

```powershell
$site = "G:\Codex项目\helldivers-wiki-localizer\output_zh\site_final"
$mirror = "G:\Codex项目\helldivers-wiki-mirror"

robocopy "$site\pages" "$mirror\pages" /MIR /XD .git /NFL /NDL /NJH /NJS /NP /R:2 /W:2
robocopy "$site\images" "$mirror\images" /MIR /XD .git /NFL /NDL /NJH /NJS /NP /R:2 /W:2
robocopy "$site\categories" "$mirror\categories" /MIR /XD .git /NFL /NDL /NJH /NJS /NP /R:2 /W:2
robocopy "$site" "$mirror" index.html patch_notes.html theme.css titles_cn.json missing_images.txt /NFL /NDL /NJH /NJS /NP /R:2 /W:2
```

robocopy 退出码 0~7 均视为成功，≥8 才是失败。

### 5.2 git 提交与推送（必须走代理）

```powershell
cd G:\Codex项目\helldivers-wiki-mirror
git config user.name "A-Creep"
git config user.email "A-Creep@users.noreply.github.com"
git add -A
git commit -m "站点增量同步 2026-08-18"
git -c http.proxy=http://127.0.0.1:7890 -c https.proxy=http://127.0.0.1:7890 push origin main
```

> 也可以直接 `python sync_update.py --push` / `--all`，脚本会自动完成上述全部动作。

---

## 6. 验证项说明

- `verify_links.py`：全站 HTML 链接/图片引用完整性检查（大小写不敏感、双重解码），有缺失即退出码 1。
- `validate_site.py --report`：输出 `validate_report.json`，按严重级别分类：
  - **high**：应有 infobox 却缺失 `druid-infobox`、页面被截断（缺 `</html>`）——**必须为 0 才能发布**
  - medium：`mw-editsection` 编辑按钮残留、教程视频图注残留
  - low：跳主页链接文案异常、中文后接英文问号等翻译残留

---

## 7. 常见问题速查

| 问题 | 现象 | 处理 |
|------|------|------|
| 页面截断 | 页面缺 `</html>`，底部内容丢失 | `python validate_site.py --report` 定位 → 删该页缓存行 → `python sync_update.py --skip-sync --invalidate "标题"` 重渲染；仍失败说明 wikitext 有模板错误 |
| 缺失 infobox | wikitext 含 `{{Infobox X}}` 但页面无 druid-infobox | 同上重渲染；`fix_broken_pages.py --dry-run` 可批量检测，`--fix` 自动删坏页缓存并重建 |
| 图片缺失 | `missing_images.txt` 记录缺失图（当前 162 条，随页面更新浮动） | 缺图基本是原站新图未下载：可先用 wiki API 按文件名补拉（`action=query&titles=File:原名&prop=imageinfo&iiprop=url&iiurlwidth=宽度` 取 thumburl 下载到 `output_zh/site/images/` 和 `site_final/images/`）；批量缺图再跑 `python build_mirror.py`（全量抓图）→ 压缩 → 重建 |
| 页面截断/内容互相覆盖 | 多个会话或代理同时跑构建管线，并发写 `output_zh/site_final/pages/` | **不要并发执行 build 管线**：同一时间只允许一个会话运行 `sync_update.py`/`build_site.py`/`apply_zh_terms.py`；如已并发，等全部进程结束后重跑一次 `python sync_update.py --skip-sync` 并校验 |
| 引导句回退（英文残留） | `build_site` 重建页面会从 `zh_cache.jsonl` 覆盖 `apply_zh_terms.py` 的后处理成果，个别 LEAD 页面（如 Drum_Magazine）可能回退为 "The X is a 中文" | `sync_update.py` 已在 terms 后自动复核 LEAD_PAGE_FIXES 18 页（`step_lead_check`），发现缺失会终止发布并提示重跑 `python apply_zh_terms.py` 或 `--skip-sync --invalidate <标题>`；手动重建后也可主动重跑 terms 复核 |
| 缓存损坏 | `build_site.py` 报 json 解析异常、渲染页数异常 | 清空 `output_zh/site_final/zh_cache.jsonl`（先备份）→ `python sync_update.py --skip-sync` 全量重建（约 25 分钟） |
| EXE 进程占用 | PyInstaller 报 `dist\Helldivers2WikiCN.exe` 被占用 | `taskkill /IM Helldivers2WikiCN.exe /F` 后重试；`sync_update.py --package` 会自动处理 |
| GitHub 推送失败 | 502 / CONNECT 404 / TLS 错误 | 确认代理端口 7890 开放；推送命令带 `-c http.proxy=http://127.0.0.1:7890`；不要设置系统代理 `ProxyEnable=1` |
| 页面从保留子集移除后仍存在 | pages/ 下残留旧 html | 手动跑 `python cleanup_site.py`（按 `titles_cn.json` 清理） |
| 英文残留 | 模板/UI 英文未翻译 | 补充 `apply_zh_terms.py` 的 TERMS 表后重跑；正文残留走翻译流程 |
| 翻译没生效 | 页面仍是英文 | `python wiki_extractor.py import translated.json` 后再 `python sync_update.py --skip-sync` |
| 同步报 429 | API 限流 | 等 30~60 秒重试；脚本内已有退避重试 |
| `sync` 报 `UNIQUE constraint failed: pages.title` | wiki_extractor.upsert_page 用 `ON CONFLICT(pageid)`，同名页不同 pageid 时会冲突 | 直接使用 `sync_update.py`（已内置 title 优先的 upsert 兼容处理）；后续可让任务 A/E 修复 wiki_extractor.py 本身 |

---

## 8. 验收自查

1. `python sync_update.py` 在无变更时秒级完成。
2. 模拟一个页面变更（或 `--invalidate` 指定页）后，`build_site.py` 输出"需汉化渲染 1 页"。
3. `validate_site.py` 高危 = 0。
4. 新会话照 README 可在 10 分钟内跑通查看状态/增量同步。

---

## 9. 给 Kimi 的站点链接规范（重要）

凡交给 Kimi（`--ask` / `--upload` / 上下文文件）的镜像站页面引用，**必须写完整 URL**：

- 正确：`https://a-creep.github.io/helldivers-wiki-cn/categories/stratagems.html`
- 错误：`categories/stratagems.html`

Kimi 会把裸的相对路径当成可访问链接直接跳转，页面实际不存在时会误判为"搜索失败/内容缺失"。

常用页面完整 URL 示例：

| 页面 | 完整 URL |
|------|----------|
| 首页 | `https://a-creep.github.io/helldivers-wiki-cn/index.html` |
| 敌人分类 | `https://a-creep.github.io/helldivers-wiki-cn/categories/enemies.html` |
| 武器分类 | `https://a-creep.github.io/helldivers-wiki-cn/categories/weapons.html` |
| 战略配备分类 | `https://a-creep.github.io/helldivers-wiki-cn/categories/stratagems.html` |
| 词条页示例 | `https://a-creep.github.io/helldivers-wiki-cn/pages/AR-23_Liberator.html` |
| 更新日志 | `https://a-creep.github.io/helldivers-wiki-cn/patch_notes.html` |

注意区分：本文档中出现的 `pages/`、`categories/`、`index.html`、`output_zh/site_final/` 等是**本地项目路径或命令参数**（robocopy、构建脚本、缓存目录），不是线上链接；只有"让 Kimi 访问站点页面"时才写完整 URL。
